---
# Keep these compatible with ansible-core in Dockerfile. CI checks the
# collections' requires_ansible metadata; Renovate proposes version updates.
collections:
  - name: ansible.posix
    version: "2.1.0"
  - name: community.general
    version: "11.4.9"
  - name: community.hashi_vault
    version: "7.1.0"
