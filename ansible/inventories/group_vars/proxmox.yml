---
pve_templates:
  - name: debian12-cloud
    vmid: 9000
    image_file: debian-12-generic-amd64.img
    datastore: local-lvm
    bridge: vmbr0
    memory: 2048
    cores: 2
    ciuser: deploy
    description: "Debian 12 cloud-init template with qemu-guest-agent (built by Ansible)"

# managed_users inherited from group_vars/all.yml
