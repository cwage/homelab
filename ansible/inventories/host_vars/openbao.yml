# Host-specific configuration for openbao VM

# Hostname override - DNS record is 'bao', not 'openbao'
hostname_override: bao

# NFS mounts for backup storage
nfs_mounts:
  - name: homelab-backups
    src: "10.10.15.4:/volume1/homelab-backups"
    path: /mnt/backups
