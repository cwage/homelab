# Backup Tooling Configuration
# Copy this file to .env and fill in values as needed
# This .env file is gitignored for security
#
# These are FALLBACK credentials for when OpenBao is not available.
# If BAO_ADDR and BAO_TOKEN are set in the root .env, credentials
# will be fetched from OpenBao instead.

# =============================================================================
# Backblaze B2 Credentials (rclone "b2" remote)
# =============================================================================

# Get these from Backblaze B2 > App Keys
RCLONE_CONFIG_B2_TYPE=b2
RCLONE_CONFIG_B2_ACCOUNT=your_account_id_here
RCLONE_CONFIG_B2_KEY=your_application_key_here

# =============================================================================
# Rclone Crypt Overlay (rclone "b2crypt" remote)
# =============================================================================

# Crypt overlay for client-side encryption
RCLONE_CONFIG_B2CRYPT_TYPE=crypt
RCLONE_CONFIG_B2CRYPT_REMOTE=b2:cwagenas-backup

# Generate with: rclone obscure <your-password>
# Or use the raw password (rclone accepts both)
RCLONE_CONFIG_B2CRYPT_PASSWORD=your_crypt_password_here

# Optional salt for filename encryption
# Generate with: rclone obscure <your-salt>
# RCLONE_CONFIG_B2CRYPT_PASSWORD2=your_crypt_salt_here
